diff --git a/fairino_hardware_v3_9_4/CMakeLists.txt b/fairino_hardware_v3_9_4/CMakeLists.txt
index abb95de..c876ccf 100644
--- a/fairino_hardware_v3_9_4/CMakeLists.txt
+++ b/fairino_hardware_v3_9_4/CMakeLists.txt
@@ -66,3 +66,8 @@ $<INSTALL_INTERFACE:include/fairino_hardware>
-# 链接库时添加rpath参数
-target_link_libraries(ros2_cmd_server "-Wl,-rpath,$ORIGIN" ${CMAKE_CURRENT_SOURCE_DIR}/libfairino/lib/libfairino.so.2.3.4)
-target_link_libraries(fairino_hardware "-Wl,-rpath,$ORIGIN" ${CMAKE_CURRENT_SOURCE_DIR}/libfairino/lib/libfairino.so.2.3.4)
+# Both targets are installed below lib/${PROJECT_NAME}; keep the SDK in the
+# parent lib directory and encode that stable relationship in RUNPATH.
+target_link_libraries(ros2_cmd_server ${CMAKE_CURRENT_SOURCE_DIR}/libfairino/lib/libfairino.so.2.3.4)
+target_link_libraries(fairino_hardware ${CMAKE_CURRENT_SOURCE_DIR}/libfairino/lib/libfairino.so.2.3.4)
+set_target_properties(ros2_cmd_server fairino_hardware PROPERTIES
+  BUILD_RPATH "${CMAKE_CURRENT_SOURCE_DIR}/libfairino/lib"
+  INSTALL_RPATH "$ORIGIN/.."
+)
@@ -103,18 +108,8 @@ install(TARGETS fairino_hardware
-#copy fairino SDK lib file to specfic path
-file(MAKE_DIRECTORY
-"${CMAKE_INSTALL_PREFIX}/lib/${PROJECT_NAME}"
-)
-
-file(COPY_FILE "libfairino/lib/libfairino.so.2.3.4"
-"${CMAKE_INSTALL_PREFIX}/lib/libfairino.so.2.3.4"
-)
-
-# file(CREATE_LINK
-# "${CMAKE_INSTALL_PREFIX}/lib/libfairino.so.2.3.0"
-# "${CMAKE_INSTALL_PREFIX}/lib/libfairino.so.2"
-# SYMBOLIC)
-
-# file(CREATE_LINK
-# "${CMAKE_INSTALL_PREFIX}/lib/libfairino.so.2"
-# "${CMAKE_INSTALL_PREFIX}/lib/libfairino.so"
-# SYMBOLIC)
+# Install the SDK and the SONAME links required by the ELF loader.
+install(FILES libfairino/lib/libfairino.so.2.3.4 DESTINATION lib)
+install(CODE "
+  execute_process(COMMAND \"${CMAKE_COMMAND}\" -E create_symlink
+    libfairino.so.2.3.4 \"${CMAKE_INSTALL_PREFIX}/lib/libfairino.so.2\")
+  execute_process(COMMAND \"${CMAKE_COMMAND}\" -E create_symlink
+    libfairino.so.2 \"${CMAKE_INSTALL_PREFIX}/lib/libfairino.so\")
+")
